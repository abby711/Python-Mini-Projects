import itertools
numbers=[8,1,2,1,3,4,9]
array=set(numbers)
array=list(array)

combinations=[]
temp=0
combinations=list(itertools.permutations(array,len(array)))

for i in combinations:
  sum=0
  #print(i)
  for j in i:
    sum=sum*10+j
  if sum%2==0 and sum>temp:
    temp=sum


print(temo)
